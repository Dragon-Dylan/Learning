#include"NEEDED.H"

int main()
{
	int key = 9;
	while(key == 9)
	{
		IndentityMenu(&key);
		getchar();
	}
	switch(key)
	{
		case 1:break;
		case 2:break;
		case 3:break;
		case 0:return 0;
	}

	printf("  Thinks for using!\n");
	return 0;
}