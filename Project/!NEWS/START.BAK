#include"NEEDED.H"

int main()
{
	int key = 9;
	IndentityMenu(&key);

	struct UserType u = {{"Visitor", "000000"}, 0};
	switch(key)
	{
		case 1:

			Login(u);
			break;
		case 2:
			Interface(u);
			break;
		case 3:
			break;
		case 4:
			SaveUser();
			main();
			break;
		case 0:
			return 0;
	}


	printf("  Thinks for using!\n");
	return 0;
}
